extends layout

block content

    .container
        .row
            .col 
                h2 #{client.last_name},  #{client.first_name}
                h5 Contact: #{client.contact_number}
                h5 Email: #{client.email}
            .col.text-end
                a.btn.btn-primary.btn-lg.m-2(href='/addNote/'+client_id, role='button') Add Session Note

        hr

    include partials/timeline
    